@model IEnumerable<RH___LW.Models.CandidatoRefTech>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CandidatoRefTechName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CandidatoRefTechName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CandidatoRefTechID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CandidatoRefTechID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CandidatoRefTechID })
        </td>
    </tr>
}

</table>
