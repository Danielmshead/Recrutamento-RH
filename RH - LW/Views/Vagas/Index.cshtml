@model IEnumerable<RH___LW.Models.Vaga>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.VagaNome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.VagaRefDetalhe.VagaRefDetalheName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.VagaNome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.VagaRefDetalhe.VagaRefDetalheName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.VagaID }) |
            @Html.ActionLink("Details", "Details", new { id=item.VagaID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.VagaID })
        </td>
    </tr>
}

</table>
