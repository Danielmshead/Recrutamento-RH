@model IEnumerable<RH___LW.Models.VagaRefDetalhe>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.VagaRefDetalheName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.VagaRefDetalheName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.VagaRefDetalheID }) |
            @Html.ActionLink("Details", "Details", new { id=item.VagaRefDetalheID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.VagaRefDetalheID })
        </td>
    </tr>
}

</table>
